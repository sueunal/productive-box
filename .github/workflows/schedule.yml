name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.af9568a697696ebc3829b596b889393b }}
          TIMEZONE: Asia/Seoul
